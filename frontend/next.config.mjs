/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Local dev: backend runs on http://localhost:8787. Override with
  // NEXT_PUBLIC_API_BASE_URL if you point at a remote backend.
};

export default nextConfig;
